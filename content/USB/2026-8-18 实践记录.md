# 任务1.在UEFI Shell下枚举所有USB设备并输出设备描述符



## 实现思路：

UsbBusDxe 在枚举阶段已经为每个逻辑设备都安装了一个 USB IO 实例，应用层按 GUID 遍历句柄即可

在AmiModulePkg/Usb/UsbBus.c中能找到如下图函数，枚举USB设备由该函数在DXE阶段完成
从该函数注释可以看出该函数用于安装USB IO 实例。

![[Pasted image 20260818155245.png]]


该函数内部可以看到
![[Pasted image 20260818164002.png]]
## 实现步骤

![[Pasted image 20260818164531.png]]
枚举系统中所有安装了 `gEfiUsbIoProtocolGuid` 的 handle。

### 2. 通过上面拿到的句柄拿到对应的协议指针，用于后面使用协议中的功能函数
![[Pasted image 20260818164655.png]]


### 3. 通过Protocol中的功能函数去读取设备描述符

![[Pasted image 20260818164748.png]]

返回 18 字节标准设备描述符。
![[Pasted image 20260818165016.png]]

### 4. 输出设备路径

![[Pasted image 20260818165655.png]]

设备路径标识设备挂在哪个控制器的哪个端口，当多台设备 VID/PID 相同时靠它区分物理位置。

### 5. 输出全部字段
![[Pasted image 20260818165206.png]]

## 验证结果

实际运行枚举出 4个USB设备，因为采用的是寻找USB IO实例的方式去实现输出USB设备描述符，所以只有接在USB总线上的设备才会被枚举出实例。将鼠标拔掉后再次枚举发现就只有3个USB设备了。

![[img_v3_0214m_8385eebe-ae06-469a-bb68-eeb8f682d76g.jpg]]![[img_v3_0214m_10172226-496a-4b05-a274-3cc3104a7bcg.jpg]]![[img_v3_0214m_23a10004-e845-473e-9dea-94a1de05e55g.jpg]]![[img_v3_0214m_04709c1b-7a5f-4019-bd47-97b6864d9eeg.jpg]]